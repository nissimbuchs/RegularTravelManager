{
  "name": "@rtm/calculation-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Calculation Engine Domain Logic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@rtm/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^18.18.0",
    "vitest": "^1.0.0"
  }
}